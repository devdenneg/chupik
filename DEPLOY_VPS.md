# 🚀 Развертывание обновления на VPS

## Быстрое развертывание (рекомендуется)

### Автоматический скрипт:

```bash
./deploy_humanization.sh
```

Скрипт автоматически:
- ✅ Создаст архив с обновленными файлами
- ✅ Скопирует на VPS
- ✅ Остановит старого бота
- ✅ Создаст backup старых файлов
- ✅ Распакует новые файлы
- ✅ Проверит синтаксис
- ✅ Запустит бота с новыми функциями

---

## Ручное развертывание

### Шаг 1: Копирование файлов

```bash
scp bot_humanization_update.tar.gz root@155.212.209.24:/root/bot/
```

### Шаг 2: Подключение к VPS

```bash
ssh root@155.212.209.24
```

### Шаг 3: Остановка бота

```bash
cd /root/bot
pkill -f "python3 bot.py"
```

### Шаг 4: Backup старых файлов

```bash
mkdir -p backup_$(date +%Y%m%d_%H%M%S)
cp bot.py persona.py backup_*/
```

### Шаг 5: Распаковка обновления

```bash
tar -xzf bot_humanization_update.tar.gz
```

### Шаг 6: Проверка синтаксиса

```bash
python3 -m py_compile mood_manager.py
python3 -m py_compile human_behavior.py
python3 -m py_compile bot.py
python3 -m py_compile persona.py
```

### Шаг 7: Запуск бота

```bash
nohup python3 bot.py > bot.log 2>&1 &
```

### Шаг 8: Проверка статуса

```bash
ps aux | grep bot.py
tail -f bot.log
```

---

## Проверка работы новых функций

### 1. Проверить настроение
Отправьте позитивные сообщения боту:
```
"ура!"
"супер!"
"класс!"
```

Бот должен стать веселее и добавлять больше эмодзи.

### 2. Проверить время суток
- Утром (6-12): бодрый тон
- Днём (12-18): обычный тон
- Вечером (18-24): расслабленный
- Ночью (0-6): сонливый, короткие ответы

### 3. Проверить паузы
После отправки сообщения должен появляться индикатор "typing..." на 2-5 секунд.

### 4. Проверить опечатки
В ~5-8% ответов должны быть опечатки. Редко бот исправляет их (~3%).

---

## Файлы настроения

Бот создаст файл `mood_state.json` для сохранения настроения между перезапусками.

Не удаляйте его, если хотите сохранить настроение бота!

---

## Откат обновления

Если что-то пошло не так:

```bash
cd /root/bot
pkill -f "python3 bot.py"

# Найти последний backup
ls -lt | grep backup_

# Восстановить файлы
cd backup_YYYYMMDD_HHMMSS/
cp * ../
cd ..

# Запустить
nohup python3 bot.py > bot.log 2>&1 &
```

---

## Логи и мониторинг

### Просмотр логов в реальном времени:
```bash
tail -f /root/bot/bot.log
```

### Проверка статуса:
```bash
ps aux | grep bot.py
```

### Перезапуск бота:
```bash
pkill -f "python3 bot.py"
nohup python3 bot.py > bot.log 2>&1 &
```

---

## Что нового?

✅ **Система настроения** - бот реагирует на эмоции и меняет поведение
✅ **Время суток** - разный тон в зависимости от времени
✅ **Паузы** - естественные задержки перед ответом
✅ **Опечатки** - иногда делает ошибки как человек
✅ **Исправления** - редко исправляет свои опечатки

Подробнее: `HUMANIZATION_GUIDE.md`

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `tail -f bot.log`
2. Проверьте процесс: `ps aux | grep bot.py`
3. Откатите обновление (инструкция выше)

✅ **Готово к использованию!**
