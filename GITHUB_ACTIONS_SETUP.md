# 🚀 Настройка автоматического деплоя через GitHub Actions

## Что это дает?

При каждом push в ветку `main` или `master` бот автоматически:
1. Собирается в архив
2. Загружается на VPS
3. Останавливает старую версию
4. Создает backup
5. Устанавливает новую версию
6. Запускает бота

## Шаг 1: Настройка SSH ключа (рекомендуется)

### На вашем компьютере:

```bash
# Генерируем SSH ключ (если еще нет)
ssh-keygen -t rsa -b 4096 -C "github-actions" -f ~/.ssh/vps_deploy

# Копируем публичный ключ на VPS
ssh-copy-id -i ~/.ssh/vps_deploy.pub root@155.212.209.24
```

### На VPS:

```bash
# Проверяем, что ключ добавлен
cat ~/.ssh/authorized_keys
```

## Шаг 2: Настройка GitHub Secrets

Перейдите в настройки вашего репозитория на GitHub:
`Settings` → `Secrets and variables` → `Actions` → `New repository secret`

Добавьте следующие секреты:

### Вариант A: С SSH ключом (безопаснее)

1. **VPS_SSH_KEY**
   ```bash
   # Скопируйте содержимое приватного ключа
   cat ~/.ssh/vps_deploy
   ```
   Вставьте весь вывод в секрет

2. **VPS_HOST**
   ```
   155.212.209.24
   ```

3. **VPS_USER**
   ```
   root
   ```

4. **BOT_DIR**
   ```
   /root/bot
   ```

### Вариант B: С паролем (проще, но менее безопасно)

Если не хотите настраивать SSH ключи:

1. **VPS_PASSWORD**
   ```
   ваш_пароль_от_vps
   ```

2. **VPS_HOST**, **VPS_USER**, **BOT_DIR** - как в варианте A

## Шаг 3: Проверка workflow

Файл `.github/workflows/deploy.yml` уже создан в репозитории.

### Ручной запуск деплоя:

1. Перейдите в `Actions` в вашем репозитории
2. Выберите `Deploy to VPS`
3. Нажмите `Run workflow`
4. Выберите ветку `main`
5. Нажмите `Run workflow`

### Автоматический запуск:

Просто сделайте commit и push в main:

```bash
git add .
git commit -m "Update bot"
git push origin main
```

Деплой запустится автоматически!

## Шаг 4: Мониторинг деплоя

### В GitHub:

1. Перейдите в `Actions`
2. Увидите список запусков
3. Кликните на запуск, чтобы увидеть логи

### На VPS:

```bash
# Подключитесь к VPS
ssh root@155.212.209.24

# Проверьте статус бота
systemctl status telegram-bot

# Смотрите логи в реальном времени
journalctl -u telegram-bot -f

# или
tail -f /root/bot/bot.log
```

## Команды управления ботом на VPS

```bash
# Статус
systemctl status telegram-bot

# Перезапуск
systemctl restart telegram-bot

# Остановка
systemctl stop telegram-bot

# Запуск
systemctl start telegram-bot

# Логи (последние 100 строк)
journalctl -u telegram-bot -n 100

# Логи в реальном времени
journalctl -u telegram-bot -f
```

## Откат к предыдущей версии

Если что-то пошло не так:

```bash
ssh root@155.212.209.24
cd /root/bot

# Остановить бота
systemctl stop telegram-bot

# Найти последний backup
ls -lt | grep backup_

# Восстановить файлы из backup
cd backup_YYYYMMDD_HHMMSS/
cp *.py ../
cp *.sh ../
cd ..

# Запустить бота
systemctl start telegram-bot
```

## Безопасность

### Важно:

- ❌ **НЕ** коммитьте `.env` файл с токенами
- ❌ **НЕ** коммитьте пароли и SSH ключи
- ✅ Используйте GitHub Secrets для всех чувствительных данных
- ✅ Используйте SSH ключи вместо паролей (когда возможно)
- ✅ `.gitignore` уже настроен для исключения секретов

### Что безопасно в репозитории:

- ✅ Код бота (Python файлы)
- ✅ Документация (.md файлы)
- ✅ Скрипты деплоя (.sh файлы)
- ✅ requirements.txt
- ✅ .env.example (пример без реальных токенов)

### Что НЕ должно попадать в репозиторий:

- ❌ .env (реальные токены и пароли)
- ❌ Логи (.log файлы)
- ❌ Данные пользователей (.json файлы)
- ❌ SSH ключи
- ❌ Архивы (.tar.gz)

## Устранение проблем

### Ошибка: "Permission denied"

```bash
# На VPS проверьте права
ls -la /root/bot
chmod +x /root/bot/*.sh
```

### Ошибка: "Bot failed to start"

```bash
# Проверьте логи
journalctl -u telegram-bot -n 100

# Проверьте .env файл на VPS
cat /root/bot/.env

# Проверьте синтаксис
cd /root/bot
python3 -m py_compile bot.py
```

### Ошибка: "sshpass: command not found"

GitHub Actions использует встроенный Ubuntu, где sshpass должен быть доступен.
Если возникла эта ошибка, используйте SSH ключ (Вариант A).

## Готово! 🎉

Теперь ваш бот будет автоматически деплоиться при каждом push в main.

Workflow:
1. Пишете код локально
2. Коммитите изменения: `git commit -am "Update"`
3. Пушите в GitHub: `git push`
4. GitHub Actions автоматически деплоит на VPS
5. Бот обновляется и перезапускается

Весь процесс занимает 1-2 минуты!
