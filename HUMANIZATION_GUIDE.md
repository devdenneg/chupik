# 🤖 Очеловечивание Бота Чупапи - Документация

Реализована полная система для придания боту более человеческого поведения. Документация по всем новым функциям.

## 📋 Что было реализовано

### 1. 🎭 Система Настроения (Mood Manager)

**Файл:** `mood_manager.py`

Управляет персистентным настроением бота на основе эмоций пользователей.

#### Категории настроения:
- **Очень радостное** (6-10): гиперактивный, много эмодзи, шутливый
- **Радостное** (2-5): веселый, дружелюбный, добавляет эмодзи
- **Нейтральное** (-1 до 1): обычное поведение
- **Грустное** (-5 до -2): сочувствующий, менее энергичный
- **Очень грустное** (-10 до -6): подавленный, тихий, поддерживающий тон

#### Механика:
```
- Положительные эмоции (joy): +1 к mood_score
- Отрицательные эмоции (sadness): -1 к mood_score
- Раздражение (anger): -0.5 к mood_score, +1 к energy
- Затухание (decay): каждые 5 минут -0.5 к абсолютному значению
- Усталость: каждые 50 сообщений -1 к energy (восстанавливается за 30 минут)
```

#### Использование:
```python
from mood_manager import MoodManager

mm = MoodManager()
# Обновить настроение
mm.update_mood(chat_id, 'joy')  # На основе сентимента

# Получить контекст для промпта
context = mm.get_mood_prompt_context(chat_id)
# Добавляется автоматически в system prompt

# Получить информацию
info = mm.get_mood_info(chat_id)
# {'score': 2, 'energy': 6.5, 'category': 'радостное', ...}
```

---

### 2. ⏰ Контекст Времени Суток (Time-Aware Context)

**Файл:** `persona.py` (функция `get_time_context()`)

Автоматически адаптирует поведение бота к времени суток.

#### Периоды:
```
🌙 Ночь (00:00-06:00)
   Тон: тихий, сонный, интимный
   Фразы: "что не спится?", "*зевает*"
   Energy: -2

☀️ Утро (06:00-12:00)
   Тон: бодрый, оптимистичный
   Фразы: "доброе утро", "как спалось?"
   Energy: +1

🌤️ День (12:00-18:00)
   Тон: деловой, активный (обычный)
   Energy: 0

🌆 Вечер (18:00-23:59)
   Тон: расслабленный, философский
   Фразы: "как прошел день?"
   Energy: -1
```

#### Использование:
```python
from persona import get_time_context

context = get_time_context()
# Добавляется автоматически в system prompt
```

---

### 3. ⌚ Естественные Паузы (Typing Indicator)

**Файл:** `human_behavior.py` (метод `typing_pause()`)

Добавляет реалистичные задержки перед ответом, имитируя время печати.

#### Механика:
```
Короткие ответы (<50 символов): 2-3 сек
Средние (50-200): 3-4 сек
Длинные (>200): 4-5 сек
Вариация: ±0.5 сек для естественности
```

#### Использование:
```python
from human_behavior import HumanBehavior

hb = HumanBehavior()
await hb.typing_pause(context, chat_id, len(response))
# Отправит "typing" action и подождет
```

---

### 4. ✏️ Опечатки и Паразиты (Natural Typos & Fillers)

**Файл:** `human_behavior.py`

#### Типы опечаток:

1. **Пропуск буквы** (2% шанс)
   - "привет" → "prvет"

2. **Замена соседней клавишей** (2% шанс)
   - "привет" → "пршвет" (ш соседняя к и)

3. **Удвоение буквы** (2% шанс)
   - "привет" → "приветт"

#### Словесные паразиты (15% шанс):
```
"эм", "ну", "типа", "короче", "в общем", "честно", "блин", "кстати"
```

#### Исправления (3% шанс):
Бот отправляет сообщение с опечаткой, затем через 1-2 секунды редактирует с "*исправил"

#### Использование:
```python
from human_behavior import HumanBehavior

hb = HumanBehavior()

# Добавить опечатки
text_with_typos, needs_fix = hb.add_typos(response, typo_chance=0.05)

# Добавить паразиты
text_with_fillers = hb.add_filler_words(text_with_typos)

# Проверить нужно ли исправление
if hb.should_fix_typo(needs_fix):
    await asyncio.sleep(random.uniform(1, 2))
    await message.edit_text(original_text + " *исправил")
```

---

## 🔧 Интеграция в Bot

Все функции автоматически интегрированы в `bot.py`:

### В функции `process_message()`:

1. **После получения сообщения:**
   ```python
   sentiment = smart_ai.detect_sentiment(user_text)
   mood_manager.update_mood(chat_id, sentiment)
   ```

2. **Перед вызовом GLM (добавляем контексты):**
   ```python
   mood_context = mood_manager.get_mood_prompt_context(chat_id)
   time_context = get_time_context()
   enhanced_prompt = SYSTEM_PROMPT + mood_context + time_context + ...
   ```

3. **Перед отправкой ответа:**
   ```python
   # Паузы
   await human_behavior.typing_pause(context, chat_id, len(response))

   # Опечатки
   response_with_typos, needs_fix = human_behavior.add_typos(response)

   # Паразиты
   response_with_fillers = human_behavior.add_filler_words(response_with_typos)

   # Отправка
   sent_message = await message.reply_text(response_with_fillers)

   # Редкое исправление
   if human_behavior.should_fix_typo(needs_fix):
       await asyncio.sleep(random.uniform(1, 2))
       await sent_message.edit_text(response + " *исправил")
   ```

---

## 📊 Файлы

### Новые файлы:
- ✅ `mood_manager.py` (250 строк) - система настроения
- ✅ `human_behavior.py` (200 строк) - паузы и опечатки
- ✅ `mood_state.json` - сохранение настроений (создается автоматически)

### Модифицированные файлы:
- ✅ `bot.py` - интеграция всех функций
- ✅ `persona.py` - функция времени суток
- ✅ `smart_ai.py` - не модифицирован (используется существующий detect_sentiment)

---

## 🎯 Примеры поведения

### Утром (6-12):
> 🌞 Доброе утро! Как спалось? Я вот только проснулся и уже полон энергии! ✨ Чем я могу помочь?

### Ночью (0-6):
> 🌙 что не спится... Я тоже не сплю, типа... *зевает* 😴 Че нужно?

### После позитивных сообщений:
> 🔥 Ого! Рад за тебя! Это реально крутые новости! Ты просто огонь! ✨🎉

### С опечатками и паразитами:
> Слушай, в общем, я тут думаю что это реально интересный вопрос, честно... 🤔
> (через 2 сек) Слушай, я думаю что это интересный вопрос! *исправил ✏️

---

## ⚙️ Настройки

### Вероятности (можно менять в `human_behavior.py`):

```python
# Опечатки (текущая: 5%)
response_with_typos, needs_fix = human_behavior.add_typos(response, typo_chance=0.05)

# Паразиты (текущая: 15%)
# В функции add_filler_words: if random.random() > 0.15

# Исправления (текущая: 3%)
# В функции should_fix_typo: random.random() < 0.03
```

### Паузы (можно менять в `human_behavior.py`):

```python
# Формула: base_delay + char_delay, макс 5 сек
base_delay = 2.0
char_delay = min(text_length * 0.02, 3.0)
total = min(base_delay + char_delay, 5.0)
```

### Decay настроения (можно менять в `mood_manager.py`):

```python
# Каждые 5 минут: -0.5 к абсолютному значению
decay_amount = (minutes_passed / 5) * 0.5

# Энергия восстанавливается за 30 минут
mood['energy'] = min(mood['energy'] + (minutes_passed / 30) * 5, 5)
```

---

## 🧪 Тестирование

### Проверка настроения:
1. Отправить позитивные сообщения: "ура!", "супер!", "класс!"
2. Бот должен стать веселее, добавлять больше эмодзи

### Проверка времени суток:
1. Разные части дня должны дать разные тоны
2. Ночью ответы должны быть короче и сонливее

### Проверка пауз:
1. После отправки команды видим "typing..." перед ответом
2. Длинные ответы ждут дольше

### Проверка опечаток:
1. В 5-8% ответов должны быть опечатки
2. В 1-2% случаев из 30 сообщений должны быть исправления

---

## 🚀 Развертывание

Просто запустите бота как обычно:

```bash
python3 bot.py
```

Все новые функции будут работать автоматически без дополнительной конфигурации.

---

## 📝 Примечания

- **Настроение** сохраняется в `mood_state.json` и восстанавливается при перезапуске бота
- **Опечатки** не добавляются в числа, ссылки или технические термины
- **Паузы** адаптируются к длине ответа, максимум 5 секунд
- **Время суток** определяется на основе местного времени сервера
- Все функции **потокобезопасны** и работают с asyncio

---

## 🎪 Итого

Теперь бот Чупапи:
- ✅ Имеет динамическое настроение, которое зависит от эмоций пользователей
- ✅ Адаптирует тон к времени суток
- ✅ Имеет естественные паузы перед ответом
- ✅ Иногда делает опечатки и добавляет словесные паразиты
- ✅ Редко исправляет свои опечатки (как реальный человек)

Все это делает бота более живым и человеческим! 🎉
