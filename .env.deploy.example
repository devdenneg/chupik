# Конфигурация для деплоя на VPS
# Скопируйте этот файл в .env.deploy и заполните реальными данными
# НЕ КОММИТЬТЕ .env.deploy в Git!

# VPS настройки
VPS_HOST=155.212.209.24
VPS_USER=root
VPS_PASSWORD=your_vps_password_here
BOT_DIR=/root/bot

# Как использовать:
# 1. Скопируйте файл: cp .env.deploy.example .env.deploy
# 2. Отредактируйте .env.deploy и укажите реальный пароль
# 3. Загрузите переменные: source .env.deploy
# 4. Запустите деплой: ./deploy.sh
